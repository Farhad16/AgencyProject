export interface Invite {
    senderEmail: string,
    guideEmail: string,
    senderType: number,
    senderName: string,
    senderPhone: string,
    senderAddress: number,
    startingDate: string,
    endingDate: string,
    places: string
}
