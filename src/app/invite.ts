export interface Invite {
    senderEmail: string,
    getEmail: string,
    senderType: number,
    senderName: string,
    senderPhone: string,
    senderTelephone: string ,
    senderCity : number,
    senderDescription : string,
}
